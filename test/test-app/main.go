package main

/*
import (
	"fmt"
	"log"

	"github.com/aryszka/config"
)

type TestOptions struct {
	Foo int
	Bar bool
	Baz []int
}

func main() {
	var o TestOptions

	c := config.New(&o)
	if err := c.Apply(); err != nil {
		log.Fatalln(c.HelpFor(err))
	} else if c.HelpRequested() {
		fmt.Println(c.Help())
	}

	fmt.Printf("options: %v\n", o)
}
*/
